# Local operator configuration template. Copy to .env.local and fill values privately.
# Bearer token for authenticated OpenAI-compatible embedding endpoints (for example LM Studio).
LM_API_TOKEN=
# Optional token for M20-A2 GitHub public repository metadata search (never persisted).
GITHUB_TOKEN=
