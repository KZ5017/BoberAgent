[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "boberagent-core"
version = "0.0.0"
description = "Core platform package for BoberAgent"
readme = "README.md"
requires-python = ">=3.12,<4.0"
dependencies = [
    "alembic>=1.16,<2",
    "boberagent-contracts",
    "boberagent-transport",
    "boberagent-transport-mcp",
    "httpx>=0.28,<1",
    "jsonschema>=4.26,<5",
    "pydantic>=2.12,<3",
    "qdrant-client>=1.15,<2",
    "sqlalchemy>=2.0,<3",
]

[project.scripts]
boberagent-core-db = "boberagent_core.persistence.migrations:main"

[tool.uv.sources]
boberagent-contracts = { workspace = true }
boberagent-transport = { workspace = true }
boberagent-transport-mcp = { workspace = true }
